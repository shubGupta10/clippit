export enum UserRole {
    USER = 'user',
    ADMIN = 'admin',
}

export enum UserPlan {
    FREE = 'free',
    PRO = 'pro',
}

export enum PlanStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
    CANCELLED = 'cancelled',
    PAST_DUE = 'past_due',
}